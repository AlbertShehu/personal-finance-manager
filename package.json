{
  "name": "finman-app",
  "version": "1.0.0",
  "description": "Personal Finance Management App",
  "scripts": {
    "build": "cd client && npm run build",
    "dev": "cd client && npm run dev"
  },
  "engines": {
    "node": "22.x"
  }
}
