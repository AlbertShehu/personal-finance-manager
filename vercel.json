{
  "buildCommand": "npm run build",
  "installCommand": "npm ci",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://api.your-backend.com/:path*" },
    { "source": "/(.*)", "destination": "/" }
  ]
}