{
  "buildCommand": "npm run build",
  "installCommand": "npm ci",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://your-backend-url.vercel.app/:path*" },
    { "source": "/(.*)", "destination": "/" }
  ]
}